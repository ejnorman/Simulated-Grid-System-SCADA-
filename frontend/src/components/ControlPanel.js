/**
 * ControlPanel — manual operator controls.
 *
 * No props needed — this component manages its own form state and calls the API directly.
 *
 * Use sendControlCommand(cmd) from api/client.js.
 * command_type options: "adjust_generation", "trip_breaker", "close_breaker"
 *
 * See docs/reference/frontend/src/components/ControlPanel.js for a reference implementation.
 */

export default function ControlPanel() {
  return <div>ControlPanel — not yet implemented</div>;
}
